// Ce projet est bootstrappé en mode "standalone" via `bootstrapApplication` (voir `src/main.ts`).
// Ce module est laissé vide pour éviter les erreurs de compilation liées aux composants standalone.
export {};
